[project]
name = "grpo-vision-transformer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.0",
    "torch>=2.6.0",
    "torchvision>=0.21.0",
]

[tool.uv.workspace]
members = ["grpo"]
